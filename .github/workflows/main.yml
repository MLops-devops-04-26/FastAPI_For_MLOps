# .github/workflows/main.yml
# Created by: Rajinikanth Vadla

name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests (if any)
      run: |
        # In a real project, you would run your tests here.
        # For now, we'll just list the files.
        ls -R

    - name: Build Docker image
      run: |
        docker build -t house-price-predictor .
        
# This is a placeholder for pushing to a container registry like Docker Hub, GCR, or ECR.
# To enable this, you would need to add secrets for your registry credentials.
#   - name: Push to Docker Hub
#     uses: docker/login-action@v1
#     with:
#       username: ${{ secrets.DOCKER_HUB_USERNAME }}
#       password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
#   - name: Push to Docker Hub
#     run: |
#       docker tag house-price-predictor:latest ${{ secrets.DOCKER_HUB_USERNAME }}/house-price-predictor:latest
#       docker push ${{ secrets.DOCKER_HUB_USERNAME }}/house-price-predictor:latest 